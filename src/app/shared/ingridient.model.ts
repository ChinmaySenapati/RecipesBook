export class Ingredient {
  //   public name: string;
  //   public amount: number;

  constructor(public name: string, public amount: number) {
    // this.name = name;  not required used shortcut by typescript
    // this.amount= amount;
  }
}
